document.getElementById('predictionForm').addEventListener('submit', function(event) {
    event.preventDefault();

    // Simulate prediction output
    <button onclick="getElementById('demo').innerHTML = Date()">Predicted Compressive Strength: 32 MPa (this is a static value for demonstration purposes)</button>

    // Here, you would normally gather form data and send it to a server for processing
    // and then handle the response to show the actual predicted value.
});
